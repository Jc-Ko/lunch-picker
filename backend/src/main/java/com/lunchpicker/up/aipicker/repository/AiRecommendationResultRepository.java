package com.lunchpicker.up.aipicker.repository;

import com.lunchpicker.up.aipicker.entity.AiRecommendationResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AiRecommendationResultRepository extends JpaRepository<AiRecommendationResult, Long> {
}
