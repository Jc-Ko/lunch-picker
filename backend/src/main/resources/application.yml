spring:
  datasource:
    url: jdbc:mysql://localhost:3306/menu_db?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: menu_writer
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  ai-datasource:
    url: jdbc:mysql://localhost:3306/ai_db?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: ai_writer
    password: password
gemini:
  api:
    key: ${GEMINI_API_KEY:your-api-key-here}
    url: https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent
server:
  port: 8080
